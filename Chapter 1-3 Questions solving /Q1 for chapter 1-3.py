# #Q1 — Basic variablesCreate three variables: server_name (a string), cpu_cores (an integer), and uptime_days (a float). 
# #Print all three using an f-string in one sentence, e.g., "Server X has Y cores and has been up for Z days".

# server_name=test server
# int CPU_core=8
# float(uptime)=4.0
#wrong code

#right one
server_name="Test"
cpu_core=8
uptime=10
print(f"Server {server_name} has {cpu_core} cores and uptime of {uptime}")



