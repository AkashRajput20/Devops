# Q2 — Split a line into fields
# Take just the first line of the log. Split it on whitespace into a list
# of words. Print the whole list, then print the date, the severity, and
# the message separately. The message is everything after the severity,
# so you will need slicing. Print it as a sentence, not a list of words.
